<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/navigation_clock"
        android:icon="@drawable/ic_clock"
        android:title="@string/clock"/>

    <item android:id="@+id/navigation_alarm"
        android:icon="@drawable/ic_alarm"
        android:title="@string/alarm"/>

    <item android:id="@+id/navigation_stopwatch"
        android:icon="@drawable/ic_stopwatch"
        android:title="@string/stopwatch"/>

    <item android:id="@+id/navigation_timer"
        android:icon="@drawable/ic_hourglass"
        android:title="@string/timer"/>

</menu>